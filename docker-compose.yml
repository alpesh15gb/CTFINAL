services:
  cartunez:
    build: .
    container_name: cartunez-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3015:3000"
    env_file: .env.local
    environment:
      - NODE_ENV=production
